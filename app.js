const bookList = document.querySelector('#book-list');

console.log('the parent node:', bookList.parentNode);
console.log('the parent element:', bookList.parentElement.parentElement);

console.log(bookList.children);